<!-- Global Modal -->
<div class="modal-overlay" id="global-modal" role="dialog" aria-modal="true" aria-label="弹窗" aria-hidden="true">
  <div class="modal-card" role="document">
    <button class="modal-close" aria-label="关闭">&times;</button>
    <div class="modal-body" id="modal-body"></div>
  </div>
</div>

<script>
  // Global modal
  const modal = document.getElementById('global-modal') as HTMLElement | null;
  const modalBody = document.getElementById('modal-body') as HTMLElement | null;
  const modalCard = modal?.querySelector('.modal-card') as HTMLElement | null;
  const modalClose = modal?.querySelector('.modal-close') as HTMLElement | null;
  let lastTrigger: HTMLElement | null = null;

  function getFocusableElements(container: HTMLElement): HTMLElement[] {
    return Array.from(
      container.querySelectorAll<HTMLElement>(
        "a[href], button:not([disabled]), textarea:not([disabled]), input:not([disabled]), select:not([disabled]), [tabindex]:not([tabindex='-1'])"
      )
    ).filter((el) => el.offsetParent !== null);
  }

  function trapFocus(e: KeyboardEvent) {
    if (!modal || e.key !== 'Tab') return;
    const focusable = getFocusableElements(modal);
    if (focusable.length === 0) return;
    const first = focusable[0];
    const last = focusable[focusable.length - 1];
    if (e.shiftKey && document.activeElement === first) {
      e.preventDefault();
      last.focus();
    } else if (!e.shiftKey && document.activeElement === last) {
      e.preventDefault();
      first.focus();
    }
  }

  function escapeHtml(value: string): string {
    return value.replace(
      /[&<>'"]/g,
      (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' })[character] || character
    );
  }

  function safeGitHubUrl(value: string | undefined, fallback: string): string {
    try {
      const url = new URL(value || fallback);
      const isGitHub = url.hostname === 'github.com' || url.hostname.endsWith('.github.com');
      return url.protocol === 'https:' && isGitHub ? url.href : fallback;
    } catch {
      return fallback;
    }
  }

  function platformIcon(platform: 'android' | 'windows'): string {
    if (platform === 'android') {
      return `
        <svg viewBox="0 0 32 32" aria-hidden="true">
          <path d="M10.2 9.4 7.8 5.8M21.8 9.4l2.4-3.6M8.4 13.2h15.2M9 13.2v8.6a2.2 2.2 0 0 0 2.2 2.2h9.6a2.2 2.2 0 0 0 2.2-2.2v-8.6M9 14H6.8v7M23 14h2.2v7M12 24v3M20 24v3" />
          <path d="M9 13.2a7 7 0 0 1 14 0" />
          <circle cx="12.5" cy="11" r=".8" class="versions-icon__fill" />
          <circle cx="19.5" cy="11" r=".8" class="versions-icon__fill" />
        </svg>`;
    }
    return `
      <svg viewBox="0 0 32 32" aria-hidden="true">
        <path class="versions-icon__fill" d="m4 6 10-1.4v10.2H4V6Zm12-1.7L28 2.6v12.2H16V4.3ZM4 17.2h10v10.2L4 26v-8.8Zm12 0h12v12.2l-12-1.7V17.2Z" />
      </svg>`;
  }

  function versionsMarkup(trigger: HTMLElement): string {
    const currentPlatform = trigger.dataset.currentPlatform === 'android' ? 'android' : 'windows';
    const otherPlatform = currentPlatform === 'android' ? 'windows' : 'android';
    const isAndroid = otherPlatform === 'android';
    const fallbackUrl = isAndroid
      ? 'https://github.com/Mai-with-u/MaiDroid/releases'
      : 'https://github.com/Mai-with-u/MaiBotOneKey/releases/latest';
    const downloadUrl = safeGitHubUrl(isAndroid ? trigger.dataset.androidUrl : trigger.dataset.windowsUrl, fallbackUrl);
    const projectUrl = isAndroid
      ? 'https://github.com/Mai-with-u/MaiDroid'
      : 'https://github.com/Mai-with-u/MaiBotOneKey';
    const version = isAndroid ? trigger.dataset.androidVersion : trigger.dataset.windowsVersion;
    const safeVersion = version ? `版本 ${escapeHtml(version)}` : '自动获取最新 Release';
    const currentName = currentPlatform === 'android' ? 'Android 版' : 'Windows 版';
    const productName = isAndroid ? 'MaiDroid Android 客户端' : 'MaiBot Windows 一键包';
    const packageName = isAndroid ? 'APK 安装包' : 'EXE 一键包';
    const description = isAndroid
      ? '在 Android 手机上直接运行麦麦客户端，适合移动设备安装与体验。'
      : '已整合运行环境与启动工具，适合在 Windows 电脑上快速完成部署。';
    const note = isAndroid
      ? '<strong>Beta 提示</strong><span>MaiDroid 目前仍在测试阶段，安装前请确认允许安装来自浏览器的应用。</span>'
      : '<strong>Windows 提示</strong><span>一键包适用于 Windows 设备；下载完成后按引导启动即可。</span>';

    return `
      <div class="versions-modal versions-modal--${otherPlatform}">
        <header class="versions-modal__header">
          <p class="versions-modal__eyebrow"><span aria-hidden="true"></span> OTHER VERSIONS</p>
          <h2 class="versions-modal__title" id="modal-title">其他版本</h2>
          <p class="versions-modal__subtitle">当前已为你优先推荐 ${currentName}，这里可以获取另一平台版本或选择高级部署方式。</p>
        </header>

        <section class="versions-feature" aria-labelledby="versions-platform-title">
          <div class="versions-feature__icon versions-icon">${platformIcon(otherPlatform)}</div>
          <div class="versions-feature__content">
            <div class="versions-feature__topline">
              <span class="versions-feature__badge">另一平台</span>
              <span class="versions-feature__version">${safeVersion}</span>
            </div>
            <h3 id="versions-platform-title">${productName}</h3>
            <p>${description}</p>
            <div class="versions-feature__actions">
              <a class="versions-download" href="${downloadUrl}" target="_blank" rel="noopener noreferrer">
                <span>立即下载 ${packageName}</span>
                <svg viewBox="0 0 20 20" aria-hidden="true"><path d="M4 10h11M11 6l4 4-4 4" /></svg>
              </a>
              <a class="versions-project" href="${projectUrl}" target="_blank" rel="noopener noreferrer">查看项目</a>
            </div>
          </div>
        </section>

        <section class="versions-methods" aria-labelledby="versions-methods-title">
          <div class="versions-methods__heading">
            <h3 id="versions-methods-title">高级部署</h3>
            <span>ADVANCED SETUP</span>
          </div>
          <div class="versions-methods__grid">
            <a class="versions-method" href="https://github.com/Mai-with-u/MaiBot/releases/latest" target="_blank" rel="noopener noreferrer">
              <span class="versions-method__icon" aria-hidden="true">
                <svg viewBox="0 0 24 24"><path d="M8 3H5a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h11a2 2 0 0 0 2-2v-3M14 3h7v7M10 14 21 3" /></svg>
              </span>
              <span class="versions-method__copy"><strong>源码安装</strong><small>自行配置运行环境与依赖</small></span>
              <span class="versions-method__arrow" aria-hidden="true">→</span>
            </a>
            <a class="versions-method versions-method--docker" href="https://docs.mai-mai.org/manual/deployment/" target="_blank" rel="noopener noreferrer">
              <span class="versions-method__icon" aria-hidden="true">
                <svg viewBox="0 0 24 24"><path d="M4 15h16M6 11h3v4H6zM10.5 11h3v4h-3zM15 11h3v4h-3zM10.5 6h3v4h-3zM3 15c.4 4 3.2 6 7.5 6 5.2 0 8.4-2.3 9.5-6 1.5 0 2.5-.8 3-2-1-.7-2.2-.8-3.4-.3C18.8 10.9 17 10 15 10" /></svg>
              </span>
              <span class="versions-method__copy"><strong>Docker 部署</strong><small>查看容器化部署完整文档</small></span>
              <span class="versions-method__arrow" aria-hidden="true">→</span>
            </a>
          </div>
        </section>

        <aside class="versions-note">
          <span class="versions-note__mark" aria-hidden="true">i</span>
          <span class="versions-note__copy">${note}</span>
        </aside>
      </div>`;
  }

  function openModal(trigger: HTMLElement) {
    if (!modal || !modalBody) return;
    lastTrigger = trigger;
    const content = trigger.getAttribute('data-modal');
    modalCard?.classList.toggle('modal-card--qq', content === 'qq');
    modalCard?.classList.toggle('modal-card--versions', content === 'versions');
    trigger.setAttribute('aria-expanded', 'true');
    if (content === 'qq') {
      modalBody.innerHTML = `
        <div class="qq-modal">
          <header class="qq-modal__header">
            <div class="qq-modal__title-wrap">
              <h2 class="qq-modal__title" id="modal-title">QQ 交流群</h2>
              <span class="qq-modal__accent" aria-hidden="true"></span>
            </div>
            <p class="qq-modal__subtitle">MaiBot 有多个 QQ 交流群，按不同用途划分</p>
          </header>

          <div class="qq-modal__content">
            <section class="qq-section qq-section--tech" aria-labelledby="qq-tech-title">
              <h3 class="qq-section__label" id="qq-tech-title">技术交流</h3>
              <div class="qq-section__grid">
                <article class="qq-card">
                  <div class="qq-card__body">
                    <div class="qq-card__title-row">
                      <span class="qq-card__dot" aria-hidden="true"></span>
                      <h4 class="qq-card__title">麦麦脑电图</h4>
                    </div>
                    <p class="qq-card__desc">技术交流 / 答疑</p>
                  </div>
                  <a class="qq-card__button" href="https://qm.qq.com/q/RzmCiRtHEW" target="_blank" rel="noopener noreferrer" aria-label="加入麦麦脑电图 QQ 群">加入群聊 →</a>
                </article>
                <article class="qq-card">
                  <div class="qq-card__body">
                    <div class="qq-card__title-row">
                      <span class="qq-card__dot" aria-hidden="true"></span>
                      <h4 class="qq-card__title">麦麦大脑磁共振</h4>
                    </div>
                    <p class="qq-card__desc">技术交流 / 答疑</p>
                  </div>
                  <a class="qq-card__button" href="https://qm.qq.com/q/VQ3XZrWgMs" target="_blank" rel="noopener noreferrer" aria-label="加入麦麦大脑磁共振 QQ 群">加入群聊 →</a>
                </article>
                <article class="qq-card">
                  <div class="qq-card__body">
                    <div class="qq-card__title-row">
                      <span class="qq-card__dot" aria-hidden="true"></span>
                      <h4 class="qq-card__title">麦麦要当 VTB</h4>
                    </div>
                    <p class="qq-card__desc">技术交流 / 答疑</p>
                  </div>
                  <a class="qq-card__button" href="https://qm.qq.com/q/wGePTl1UyY" target="_blank" rel="noopener noreferrer" aria-label="加入麦麦要当 VTB QQ 群">加入群聊 →</a>
                </article>
              </div>
            </section>

            <section class="qq-section qq-section--chat" aria-labelledby="qq-chat-title">
              <h3 class="qq-section__label" id="qq-chat-title">闲聊吹水</h3>
              <div class="qq-section__grid">
                <article class="qq-card">
                  <div class="qq-card__body">
                    <div class="qq-card__title-row">
                      <span class="qq-card__dot" aria-hidden="true"></span>
                      <h4 class="qq-card__title">麦麦之闲聊群</h4>
                    </div>
                    <p class="qq-card__desc">仅限闲聊，不答疑</p>
                  </div>
                  <a class="qq-card__button" href="https://qm.qq.com/q/JxvHZnxyec" target="_blank" rel="noopener noreferrer" aria-label="加入麦麦之闲聊群 QQ 群">加入群聊 →</a>
                </article>
              </div>
            </section>

            <section class="qq-section qq-section--plugin" aria-labelledby="qq-plugin-title">
              <h3 class="qq-section__label" id="qq-plugin-title">插件开发</h3>
              <div class="qq-section__grid">
                <article class="qq-card">
                  <div class="qq-card__body">
                    <div class="qq-card__title-row">
                      <span class="qq-card__dot" aria-hidden="true"></span>
                      <h4 class="qq-card__title">插件开发群</h4>
                    </div>
                    <p class="qq-card__desc">进阶开发与测试</p>
                  </div>
                  <a class="qq-card__button" href="https://qm.qq.com/q/1036092828" target="_blank" rel="noopener noreferrer" aria-label="加入插件开发群 QQ 群">加入群聊 →</a>
                </article>
              </div>
            </section>
          </div>

          <aside class="qq-tip">
            <span class="qq-tip__icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" width="20" height="20" fill="currentColor">
                <path d="M9 21h6v-1.5H9V21Zm3-19a7 7 0 0 0-4.18 12.62c.55.41.93.98 1.08 1.63l.16.75h5.88l.16-.75c.15-.65.53-1.22 1.08-1.63A7 7 0 0 0 12 2Zm2.98 10.99a4.2 4.2 0 0 0-1.48 2.01h-3a4.2 4.2 0 0 0-1.48-2.01A5 5 0 1 1 14.98 13ZM9 18.5h6V17H9v1.5Z" />
              </svg>
            </span>
            <div class="qq-tip__content">
              <strong>提示</strong>
              <p>技术问题请优先在技术交流群中提问，闲聊群不提供技术答疑。如果你在开发 MaiBot 插件，欢迎加入插件开发群交流。</p>
            </div>
          </aside>
        </div>`;
    } else if (content === 'versions') {
      modalBody.innerHTML = versionsMarkup(trigger);
    } else {
      modalBody.innerHTML =
        '<h2 id="modal-title" style="text-align:center;font-size:20px;padding:20px">呃啊还没做……</h2>';
    }
    modal.classList.add('is-open');
    document.documentElement.classList.add('modal-open');
    modal.removeAttribute('aria-label');
    modal.setAttribute('aria-labelledby', 'modal-title');
    modal.setAttribute('aria-hidden', 'false');
    document.addEventListener('keydown', handleKeyDown);
    requestAnimationFrame(() => modalClose?.focus());
  }

  function closeModal() {
    if (!modal) return;
    modal.classList.remove('is-open');
    document.documentElement.classList.remove('modal-open');
    modal.removeAttribute('aria-labelledby');
    modal.setAttribute('aria-label', '弹窗');
    modal.setAttribute('aria-hidden', 'true');
    modalCard?.classList.remove('modal-card--qq', 'modal-card--versions');
    document.removeEventListener('keydown', handleKeyDown);
    if (modalBody) modalBody.innerHTML = '';
    lastTrigger?.setAttribute('aria-expanded', 'false');
    lastTrigger?.focus();
    lastTrigger = null;
  }

  function handleKeyDown(e: KeyboardEvent) {
    if (e.key === 'Escape') {
      closeModal();
      return;
    }
    trapFocus(e);
  }

  document.addEventListener('click', (e) => {
    const trigger = (e.target as HTMLElement).closest<HTMLElement>('[data-modal]');
    if (trigger) {
      e.preventDefault();
      openModal(trigger);
      return;
    }
    if ((e.target as HTMLElement).closest('.modal-close') || (e.target as HTMLElement).matches('.modal-overlay')) {
      closeModal();
    }
  });
</script>

<style>
  /* === Global Modal === */
  .modal-overlay {
    position: fixed;
    inset: 0;
    z-index: 1000;
    background: rgba(0, 0, 0, 0.5);
    backdrop-filter: blur(4px);
    display: flex;
    align-items: center;
    justify-content: center;
    padding-top: max(16px, var(--safe-top));
    padding-right: max(16px, var(--safe-right));
    padding-bottom: max(16px, var(--safe-bottom));
    padding-left: max(16px, var(--safe-left));
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.25s ease;
  }

  .modal-overlay.is-open {
    opacity: 1;
    pointer-events: auto;
  }

  .modal-card {
    position: relative;
    background: rgba(255, 255, 255, 0.95);
    border-radius: 20px;
    padding: 32px 36px;
    width: min(560px, 100%);
    max-height: 80vh;
    max-height: min(80vh, calc(100dvh - max(32px, var(--safe-top)) - max(32px, var(--safe-bottom))));
    overflow-y: auto;
    scrollbar-width: none;
    -ms-overflow-style: none;
    box-shadow: 0 16px 48px rgba(0, 0, 0, 0.2);
    backdrop-filter: blur(20px);
  }

  .modal-card::-webkit-scrollbar {
    width: 0;
    height: 0;
  }

  :global([data-theme='dark']) .modal-card {
    background: rgba(30, 30, 30, 0.95);
    color: #f9fafc;
  }

  .modal-close {
    position: absolute;
    top: 12px;
    right: 16px;
    width: 32px;
    height: 32px;
    border: none;
    background: transparent;
    font-size: 24px;
    color: #a2abbc;
    cursor: pointer;
    border-radius: 8px;
    transition: all var(--transition-fast);
    line-height: 1;
  }

  .modal-close:hover {
    background: rgba(0, 0, 0, 0.06);
    color: #000;
  }

  :global([data-theme='dark']) .modal-close:hover {
    background: rgba(255, 255, 255, 0.08);
    color: #f9fafc;
  }

  .modal-card--qq {
    background: #ffffff;
    border-radius: 24px;
    padding: 40px;
    max-width: 760px;
    width: min(760px, 100%);
    box-shadow: 0 20px 20px rgba(0, 0, 0, 0.1);
    backdrop-filter: none;
  }

  .modal-card--qq .modal-close {
    top: 40px;
    right: 40px;
    display: grid;
    place-items: center;
    background: #f0f2f5;
    border-radius: 16px;
    color: #a2abbc;
    font-size: 0;
  }

  .modal-card--qq .modal-close::before,
  .modal-card--qq .modal-close::after {
    content: '';
    position: absolute;
    width: 14px;
    height: 2px;
    border-radius: 999px;
    background: currentColor;
  }

  .modal-card--qq .modal-close::before {
    transform: rotate(45deg);
  }

  .modal-card--qq .modal-close::after {
    transform: rotate(-45deg);
  }

  .modal-card--qq .modal-close:hover {
    background: #e8ebf0;
    color: #121831;
  }

  :global([data-theme='dark']) .modal-card--qq {
    background: #ffffff;
    color: #121831;
  }

  :global([data-theme='dark']) .modal-card--qq .modal-close:hover {
    background: #e8ebf0;
    color: #121831;
  }

  .modal-card--qq .modal-body {
    line-height: 1.5;
  }

  .modal-card--qq .modal-body h2 {
    margin: 0;
    font-size: 28px;
  }

  .modal-card--qq .modal-body a.qq-card__button {
    color: #ffffff;
  }

  .qq-modal {
    display: flex;
    flex-direction: column;
    gap: 32px;
    color: #121831;
  }

  .qq-modal__header {
    display: flex;
    flex-direction: column;
    gap: 12px;
    padding-right: 48px;
  }

  .qq-modal__title-wrap {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 4px;
  }

  .qq-modal__title {
    margin: 0;
    font-family: var(--font-display);
    font-size: 28px;
    font-weight: 700;
    line-height: 1;
    color: #121831;
  }

  .qq-modal__accent {
    width: 60px;
    height: 4px;
    border-radius: 2px;
    background: #e97f0f;
  }

  .qq-modal__subtitle {
    margin: 0;
    font-size: 15px;
    line-height: 1.4;
    color: #a2abbc;
  }

  .qq-modal__content {
    display: flex;
    flex-direction: column;
    gap: 24px;
  }

  .qq-section {
    --qq-accent: #e97f0f;
    --qq-accent-rgb: 233, 127, 15;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 16px;
  }

  .qq-section--chat {
    --qq-accent: #30ccef;
    --qq-accent-rgb: 48, 204, 239;
  }

  .qq-section--plugin {
    --qq-accent: #80de41;
    --qq-accent-rgb: 128, 222, 65;
  }

  .qq-section__label {
    margin: 0;
    padding: 4px 16px 4px 12px;
    border-left: 4px solid var(--qq-accent);
    border-radius: 4px;
    background: rgba(var(--qq-accent-rgb), 0.08);
    color: var(--qq-accent);
    font-family: var(--font-display);
    font-size: 14px;
    font-weight: 700;
    line-height: 1.4;
    white-space: nowrap;
  }

  .qq-section__grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 12px;
    width: 100%;
  }

  .qq-card {
    position: relative;
    display: flex;
    min-width: 0;
    min-height: 152px;
    flex-direction: column;
    justify-content: space-between;
    gap: 12px;
    overflow: hidden;
    padding: 20px;
    border: 1px solid rgba(var(--qq-accent-rgb), 0.12);
    border-radius: 16px;
    background:
      radial-gradient(circle at 100% 0%, rgba(var(--qq-accent-rgb), 0.18), transparent 38%),
      linear-gradient(135deg, rgba(var(--qq-accent-rgb), 0.1), rgba(var(--qq-accent-rgb), 0.02) 45%, #ffffff 100%);
  }

  .qq-card::before {
    content: '';
    position: absolute;
    top: -58px;
    left: -34px;
    width: 210px;
    height: 124px;
    border-radius: 999px;
    border: 1px solid rgba(var(--qq-accent-rgb), 0.14);
    background: rgba(var(--qq-accent-rgb), 0.06);
    transform: rotate(-12deg);
  }

  .qq-card::after {
    content: '';
    position: absolute;
    right: -42px;
    bottom: -54px;
    width: 132px;
    height: 132px;
    border-radius: 36px;
    background: rgba(var(--qq-accent-rgb), 0.08);
    transform: rotate(18deg);
  }

  .qq-card__body,
  .qq-card__button {
    position: relative;
    z-index: 1;
  }

  .qq-card__body {
    display: flex;
    min-width: 0;
    flex-direction: column;
    gap: 6px;
  }

  .qq-card__title-row {
    display: flex;
    min-width: 0;
    align-items: center;
    gap: 8px;
  }

  .qq-card__dot {
    width: 8px;
    height: 8px;
    flex: 0 0 auto;
    border-radius: 50%;
    background: var(--qq-accent);
  }

  .qq-card__title {
    min-width: 0;
    margin: 0;
    overflow: hidden;
    color: #121831;
    font-family: var(--font-display);
    font-size: 16px;
    font-weight: 700;
    line-height: 1.35;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .qq-card__desc {
    margin: 0;
    color: #a2abbc;
    font-size: 13px;
    line-height: 1.45;
  }

  .qq-card__button {
    display: inline-flex;
    width: fit-content;
    align-items: center;
    justify-content: center;
    border: 0;
    border-radius: 8px;
    background: var(--qq-accent);
    color: #ffffff;
    cursor: pointer;
    font-family: var(--font-display);
    font-size: 12px;
    font-weight: 700;
    line-height: 1.35;
    padding: 6px 12px;
    text-decoration: none;
    transition:
      transform var(--transition-fast),
      filter var(--transition-fast),
      background-color var(--transition-fast);
  }

  .qq-card__button:hover,
  .qq-card__button:focus-visible {
    color: #ffffff;
    filter: brightness(0.96);
    transform: translateY(-1px);
  }

  .qq-card__button.is-copied {
    filter: brightness(1.02);
  }

  .qq-tip {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    width: 100%;
    padding: 16px;
    border-left: 4px solid #e97f0f;
    border-radius: 8px;
    background: #fff5e6;
  }

  .qq-tip__icon {
    display: grid;
    width: 24px;
    height: 24px;
    flex: 0 0 auto;
    place-items: center;
    color: #e97f0f;
  }

  .qq-tip__content {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .qq-tip__content strong {
    color: #e97f0f;
    font-family: var(--font-display);
    font-size: 14px;
    line-height: 1.3;
  }

  .qq-tip__content p {
    margin: 0;
    color: #121831;
    font-size: 13px;
    line-height: 1.5;
  }

  @media (max-width: 760px) {
    .modal-card--qq {
      padding: 28px 22px;
      max-height: 86vh;
      max-height: min(86vh, calc(100dvh - max(24px, var(--safe-top)) - max(24px, var(--safe-bottom))));
    }

    .modal-card--qq .modal-close {
      top: 24px;
      right: 22px;
    }

    .qq-modal {
      gap: 24px;
    }

    .qq-section__grid {
      grid-template-columns: 1fr;
    }

    .qq-card {
      min-height: 140px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .qq-card__button {
      transition: none;
    }

    .qq-card__button:hover,
    .qq-card__button:focus-visible {
      transform: none;
    }
  }

  .modal-body {
    font-size: 14px;
    line-height: 1.7;
  }
  .modal-body h2 {
    font-size: 20px;
    margin-bottom: 12px;
  }
  .modal-body table {
    width: 100%;
    border-collapse: collapse;
    margin: 16px 0;
  }
  .modal-body th,
  .modal-body td {
    padding: 8px 10px;
    border-bottom: 1px solid rgba(0, 0, 0, 0.08);
    text-align: left;
    vertical-align: top;
  }
  .modal-body th {
    font-weight: 700;
    white-space: nowrap;
  }
  :global([data-theme='dark']) .modal-body th,
  :global([data-theme='dark']) .modal-body td {
    border-color: rgba(255, 255, 255, 0.08);
  }
  .modal-body a:not(.qq-card__button) {
    color: var(--c-primary);
  }
</style>

<style is:global>
  .modal-card.modal-card--qq {
    background: var(--c-bg);
    color: var(--c-text);
  }

  [data-theme='dark'] .modal-card.modal-card--qq {
    background: #1e1e1e;
    color: #f9fafc;
  }

  .modal-card.modal-card--qq .modal-close.modal-close {
    transition: none;
  }

  [data-theme='dark'] .modal-card.modal-card--qq .modal-close.modal-close {
    background: #2b2f36;
    color: #8f98a8;
  }

  [data-theme='dark'] .modal-card.modal-card--qq .modal-close.modal-close:hover {
    background: #343943;
    color: #c8ced8;
  }

  .modal-card--qq .modal-body {
    line-height: 1.5;
  }

  .modal-card--qq .modal-body h2 {
    margin: 0;
    font-size: 28px;
  }

  .modal-card--qq .modal-body a.qq-card__button {
    color: #ffffff;
  }

  .qq-modal {
    display: flex;
    flex-direction: column;
    gap: 32px;
    color: #121831;
  }

  [data-theme='dark'] .qq-modal {
    color: #f9fafc;
  }

  .qq-modal__header {
    display: flex;
    flex-direction: column;
    gap: 12px;
    padding-right: 48px;
  }

  .qq-modal__title-wrap {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 4px;
  }

  .qq-modal__title {
    margin: 0;
    font-family: var(--font-display);
    font-size: 28px;
    font-weight: 700;
    line-height: 1;
    color: #121831;
  }

  [data-theme='dark'] .qq-modal__title {
    color: #f9fafc;
  }

  .qq-modal__accent {
    width: 60px;
    height: 4px;
    border-radius: 2px;
    background: #e97f0f;
  }

  .qq-modal__subtitle {
    margin: 0;
    font-size: 15px;
    line-height: 1.4;
    color: #a2abbc;
  }

  [data-theme='dark'] .qq-modal__subtitle {
    color: #8a93a3;
  }

  .qq-modal__content {
    display: flex;
    flex-direction: column;
    gap: 24px;
  }

  .qq-section {
    --qq-accent: #e97f0f;
    --qq-accent-rgb: 233, 127, 15;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 16px;
  }

  .qq-section--chat {
    --qq-accent: #30ccef;
    --qq-accent-rgb: 48, 204, 239;
  }

  .qq-section--plugin {
    --qq-accent: #80de41;
    --qq-accent-rgb: 128, 222, 65;
  }

  .qq-section__label {
    margin: 0;
    padding: 4px 16px 4px 12px;
    border-left: 4px solid var(--qq-accent);
    border-radius: 4px;
    background: rgba(var(--qq-accent-rgb), 0.08);
    color: var(--qq-accent);
    font-family: var(--font-display);
    font-size: 14px;
    font-weight: 700;
    line-height: 1.4;
    white-space: nowrap;
  }

  [data-theme='dark'] .qq-section__label {
    background: rgba(var(--qq-accent-rgb), 0.14);
  }

  .qq-section__grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 12px;
    width: 100%;
  }

  .qq-card {
    position: relative;
    display: flex;
    min-width: 0;
    min-height: 152px;
    flex-direction: column;
    justify-content: space-between;
    gap: 12px;
    overflow: hidden;
    padding: 20px 24px 20px 20px;
    border: 1px solid rgba(var(--qq-accent-rgb), 0.18);
    border-radius: 8px;
    background: rgba(var(--qq-accent-rgb), 0.08);
    clip-path: polygon(0 0, 100% 0, 100% calc(100% - 18px), calc(100% - 18px) 100%, 0 100%);
  }

  [data-theme='dark'] .qq-card {
    border-color: rgba(var(--qq-accent-rgb), 0.26);
    background: rgba(var(--qq-accent-rgb), 0.12);
  }

  .qq-card__body,
  .qq-card__button {
    position: relative;
    z-index: 1;
  }

  .qq-card__body {
    display: flex;
    min-width: 0;
    flex-direction: column;
    gap: 6px;
  }

  .qq-card__title-row {
    display: flex;
    min-width: 0;
    align-items: center;
    gap: 8px;
  }

  .qq-card__dot {
    width: 8px;
    height: 8px;
    flex: 0 0 auto;
    border-radius: 50%;
    background: var(--qq-accent);
  }

  .qq-card__title {
    min-width: 0;
    margin: 0;
    overflow: hidden;
    color: #121831;
    font-family: var(--font-display);
    font-size: 16px;
    font-weight: 700;
    line-height: 1.35;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  [data-theme='dark'] .qq-card__title {
    color: #f9fafc;
  }

  .qq-card__desc {
    margin: 0;
    color: #a2abbc;
    font-size: 13px;
    line-height: 1.45;
  }

  [data-theme='dark'] .qq-card__desc {
    color: #9aa3b2;
  }

  .qq-card__button {
    display: inline-flex;
    width: fit-content;
    align-items: center;
    justify-content: center;
    border: 0;
    border-radius: 8px;
    background: var(--qq-accent);
    color: #ffffff;
    cursor: pointer;
    font-family: var(--font-display);
    font-size: 12px;
    font-weight: 700;
    line-height: 1.35;
    padding: 6px 12px;
    text-decoration: none;
    transition:
      transform var(--transition-fast),
      filter var(--transition-fast),
      background-color var(--transition-fast);
  }

  .qq-card__button:hover,
  .qq-card__button:focus-visible {
    color: #ffffff;
    filter: brightness(0.96);
    transform: translateY(-1px);
  }

  .qq-card__button.is-copied {
    filter: brightness(1.02);
  }

  .qq-tip {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    width: 100%;
    padding: 16px;
    border-left: 4px solid #e97f0f;
    border-radius: 8px;
    background: #fff5e6;
  }

  [data-theme='dark'] .qq-tip {
    background: rgba(233, 127, 15, 0.14);
  }

  .qq-tip__icon {
    display: grid;
    width: 24px;
    height: 24px;
    flex: 0 0 auto;
    place-items: center;
    color: #e97f0f;
  }

  .qq-tip__content {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .qq-tip__content strong {
    color: #e97f0f;
    font-family: var(--font-display);
    font-size: 14px;
    line-height: 1.3;
  }

  .qq-tip__content p {
    margin: 0;
    color: #121831;
    font-size: 13px;
    line-height: 1.5;
  }

  [data-theme='dark'] .qq-tip__content p {
    color: #f9fafc;
  }

  @media (max-width: 760px) {
    .modal-card.modal-card--qq {
      padding: 28px 22px;
      max-height: 86vh;
      max-height: min(86vh, calc(100dvh - max(24px, var(--safe-top)) - max(24px, var(--safe-bottom))));
    }

    .modal-card--qq .modal-close {
      top: 24px;
      right: 22px;
    }

    .qq-modal {
      gap: 24px;
    }

    .qq-section__grid {
      grid-template-columns: 1fr;
    }

    .qq-card {
      min-height: 140px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .qq-card__button {
      transition: none;
    }

    .qq-card__button:hover,
    .qq-card__button:focus-visible {
      transform: none;
    }
  }

  html.modal-open {
    overflow: hidden;
  }

  .modal-card.modal-card--versions {
    width: min(760px, 100%);
    max-height: min(88vh, calc(100dvh - max(32px, var(--safe-top)) - max(32px, var(--safe-bottom))));
    overflow-x: hidden;
    padding: 0;
    border: 1px solid rgba(18, 24, 49, 0.08);
    border-radius: 24px;
    background: #ffffff;
    color: #121831;
    box-shadow: 0 24px 72px rgba(18, 24, 49, 0.22);
    backdrop-filter: none;
  }

  [data-theme='dark'] .modal-card.modal-card--versions {
    border-color: rgba(255, 255, 255, 0.08);
    background: #1e2026;
    color: #f9fafc;
  }

  .modal-card--versions .modal-close {
    top: 28px;
    right: 32px;
    z-index: 4;
    display: grid;
    width: 40px;
    height: 40px;
    place-items: center;
    border: 1px solid rgba(18, 24, 49, 0.06);
    border-radius: 12px;
    background: #f1f3f6;
    color: #8f98a8;
    font-size: 0;
  }

  .modal-card--versions .modal-close::before,
  .modal-card--versions .modal-close::after {
    content: '';
    position: absolute;
    width: 15px;
    height: 2px;
    border-radius: 999px;
    background: currentColor;
  }

  .modal-card--versions .modal-close::before {
    transform: rotate(45deg);
  }

  .modal-card--versions .modal-close::after {
    transform: rotate(-45deg);
  }

  .modal-card--versions .modal-close:hover,
  .modal-card--versions .modal-close:focus-visible {
    border-color: rgba(233, 127, 15, 0.18);
    background: #fff4e8;
    color: #e97f0f;
  }

  [data-theme='dark'] .modal-card--versions .modal-close {
    border-color: rgba(255, 255, 255, 0.08);
    background: #2b2f36;
    color: #9ca5b5;
  }

  [data-theme='dark'] .modal-card--versions .modal-close:hover,
  [data-theme='dark'] .modal-card--versions .modal-close:focus-visible {
    border-color: rgba(245, 148, 25, 0.3);
    background: rgba(245, 148, 25, 0.12);
    color: #f59419;
  }

  .modal-card--versions .modal-body {
    font-size: 14px;
    line-height: 1.5;
  }

  .versions-modal {
    --versions-accent: #30ccef;
    --versions-accent-rgb: 48, 204, 239;
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 28px;
    padding: 40px;
    overflow: hidden;
  }

  .versions-modal--android {
    --versions-accent: #80de41;
    --versions-accent-rgb: 128, 222, 65;
  }

  .versions-modal::before {
    content: '';
    position: absolute;
    top: -160px;
    right: -130px;
    width: 360px;
    height: 360px;
    border: 1px solid rgba(233, 127, 15, 0.1);
    border-radius: 50%;
    box-shadow:
      0 0 0 44px rgba(233, 127, 15, 0.025),
      0 0 0 88px rgba(233, 127, 15, 0.018);
    pointer-events: none;
  }

  .versions-modal__header,
  .versions-feature,
  .versions-methods,
  .versions-note {
    position: relative;
    z-index: 1;
  }

  .versions-modal__header {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 10px;
    padding-right: 56px;
  }

  .versions-modal__eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    margin: 0;
    color: #e97f0f;
    font-family: var(--font-display);
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.14em;
    line-height: 1;
  }

  .versions-modal__eyebrow span {
    width: 24px;
    height: 3px;
    border-radius: 999px;
    background: currentColor;
  }

  .versions-modal__title {
    margin: 0;
    color: #121831;
    font-family: var(--font-display);
    font-size: 30px;
    font-weight: 700;
    line-height: 1.15;
  }

  [data-theme='dark'] .versions-modal__title {
    color: #f9fafc;
  }

  .versions-modal__subtitle {
    max-width: 560px;
    margin: 0;
    color: #8f98a8;
    font-size: 14px;
    line-height: 1.6;
  }

  [data-theme='dark'] .versions-modal__subtitle {
    color: #a3acbb;
  }

  .versions-feature {
    display: grid;
    grid-template-columns: 92px minmax(0, 1fr);
    align-items: center;
    gap: 24px;
    min-height: 220px;
    overflow: hidden;
    padding: 28px 32px 30px;
    border: 1px solid rgba(var(--versions-accent-rgb), 0.3);
    border-radius: 16px;
    background:
      linear-gradient(135deg, rgba(var(--versions-accent-rgb), 0.16), rgba(var(--versions-accent-rgb), 0.04) 58%),
      #fbfcfd;
    clip-path: polygon(0 0, 100% 0, 100% calc(100% - 24px), calc(100% - 24px) 100%, 0 100%);
  }

  .versions-feature::after {
    content: '';
    position: absolute;
    right: -54px;
    bottom: -76px;
    width: 190px;
    height: 190px;
    border: 32px solid rgba(var(--versions-accent-rgb), 0.08);
    border-radius: 50%;
    pointer-events: none;
  }

  [data-theme='dark'] .versions-feature {
    border-color: rgba(var(--versions-accent-rgb), 0.32);
    background:
      linear-gradient(135deg, rgba(var(--versions-accent-rgb), 0.18), rgba(var(--versions-accent-rgb), 0.05) 58%),
      #24272e;
  }

  .versions-feature__icon {
    position: relative;
    z-index: 1;
    display: grid;
    width: 92px;
    height: 92px;
    place-items: center;
    border: 1px solid rgba(var(--versions-accent-rgb), 0.28);
    border-radius: 24px;
    background: rgba(255, 255, 255, 0.84);
    color: var(--versions-accent);
    box-shadow: 0 12px 24px rgba(var(--versions-accent-rgb), 0.14);
    transform: rotate(-3deg);
  }

  [data-theme='dark'] .versions-feature__icon {
    background: rgba(18, 24, 49, 0.58);
  }

  .versions-icon svg {
    width: 52px;
    height: 52px;
    overflow: visible;
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.8;
  }

  .versions-icon .versions-icon__fill {
    fill: currentColor;
    stroke: none;
  }

  .versions-feature__content {
    position: relative;
    z-index: 1;
    display: flex;
    min-width: 0;
    flex-direction: column;
    align-items: flex-start;
    gap: 8px;
  }

  .versions-feature__topline {
    display: flex;
    width: 100%;
    flex-wrap: wrap;
    align-items: center;
    gap: 8px 12px;
  }

  .versions-feature__badge {
    display: inline-flex;
    min-height: 24px;
    align-items: center;
    padding: 3px 10px;
    border-radius: 999px;
    background: var(--versions-accent);
    color: #121831;
    font-family: var(--font-display);
    font-size: 11px;
    font-weight: 700;
    line-height: 1;
  }

  .versions-feature__version {
    color: #8f98a8;
    font-size: 12px;
    font-weight: 600;
  }

  [data-theme='dark'] .versions-feature__version {
    color: #a3acbb;
  }

  .versions-feature h3 {
    margin: 2px 0 0;
    color: #121831;
    font-family: var(--font-display);
    font-size: 22px;
    font-weight: 700;
    line-height: 1.25;
  }

  [data-theme='dark'] .versions-feature h3 {
    color: #f9fafc;
  }

  .versions-feature p {
    max-width: 470px;
    margin: 0;
    color: #737d8e;
    font-size: 13px;
    line-height: 1.55;
  }

  [data-theme='dark'] .versions-feature p {
    color: #aab2c0;
  }

  .versions-feature__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 8px;
  }

  .modal-card--versions .versions-download,
  .modal-card--versions .versions-project {
    display: inline-flex;
    min-height: 44px;
    align-items: center;
    justify-content: center;
    border-radius: 10px;
    font-family: var(--font-display);
    font-size: 13px;
    font-weight: 700;
    line-height: 1.2;
    text-decoration: none;
    transition:
      transform var(--transition-fast),
      box-shadow var(--transition-fast),
      background-color var(--transition-fast);
  }

  .modal-card--versions .versions-download {
    gap: 8px;
    padding: 11px 16px;
    background: var(--versions-accent);
    color: #121831;
    box-shadow: 0 8px 18px rgba(var(--versions-accent-rgb), 0.2);
  }

  .versions-download svg {
    width: 18px;
    height: 18px;
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.8;
  }

  .modal-card--versions .versions-project {
    padding: 10px 15px;
    border: 1px solid rgba(18, 24, 49, 0.12);
    background: rgba(255, 255, 255, 0.7);
    color: #596274;
  }

  [data-theme='dark'] .modal-card--versions .versions-project {
    border-color: rgba(255, 255, 255, 0.12);
    background: rgba(255, 255, 255, 0.04);
    color: #d5dae2;
  }

  .modal-card--versions .versions-download:hover,
  .modal-card--versions .versions-download:focus-visible,
  .modal-card--versions .versions-project:hover,
  .modal-card--versions .versions-project:focus-visible {
    transform: translateY(-2px);
  }

  .modal-card--versions .versions-download:hover,
  .modal-card--versions .versions-download:focus-visible {
    color: #121831;
    box-shadow: 0 12px 24px rgba(var(--versions-accent-rgb), 0.28);
  }

  .modal-card--versions .versions-project:hover,
  .modal-card--versions .versions-project:focus-visible {
    background: #ffffff;
    color: #121831;
  }

  [data-theme='dark'] .modal-card--versions .versions-project:hover,
  [data-theme='dark'] .modal-card--versions .versions-project:focus-visible {
    background: rgba(255, 255, 255, 0.09);
    color: #ffffff;
  }

  .versions-methods {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .versions-methods__heading {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 12px;
  }

  .versions-methods__heading h3 {
    margin: 0;
    color: #121831;
    font-family: var(--font-display);
    font-size: 16px;
    font-weight: 700;
  }

  [data-theme='dark'] .versions-methods__heading h3 {
    color: #f9fafc;
  }

  .versions-methods__heading > span {
    color: #b0b7c3;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.12em;
  }

  .versions-methods__grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 12px;
  }

  .modal-card--versions .versions-method {
    display: grid;
    min-width: 0;
    min-height: 82px;
    grid-template-columns: 42px minmax(0, 1fr) auto;
    align-items: center;
    gap: 12px;
    padding: 16px;
    border: 1px solid rgba(18, 24, 49, 0.08);
    border-radius: 12px;
    background: #f7f8fa;
    color: #121831;
    text-decoration: none;
    transition:
      border-color var(--transition-fast),
      background-color var(--transition-fast),
      transform var(--transition-fast);
  }

  [data-theme='dark'] .modal-card--versions .versions-method {
    border-color: rgba(255, 255, 255, 0.08);
    background: #262930;
    color: #f9fafc;
  }

  .modal-card--versions .versions-method:hover,
  .modal-card--versions .versions-method:focus-visible {
    border-color: rgba(233, 127, 15, 0.28);
    background: #fff8f0;
    color: #121831;
    transform: translateY(-2px);
  }

  [data-theme='dark'] .modal-card--versions .versions-method:hover,
  [data-theme='dark'] .modal-card--versions .versions-method:focus-visible {
    border-color: rgba(245, 148, 25, 0.32);
    background: rgba(245, 148, 25, 0.1);
    color: #ffffff;
  }

  .versions-method__icon {
    display: grid;
    width: 42px;
    height: 42px;
    place-items: center;
    border-radius: 10px;
    background: rgba(233, 127, 15, 0.1);
    color: #e97f0f;
  }

  .versions-method--docker .versions-method__icon {
    background: rgba(48, 204, 239, 0.12);
    color: #1ab8dd;
  }

  .versions-method__icon svg {
    width: 22px;
    height: 22px;
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.7;
  }

  .versions-method__copy {
    display: flex;
    min-width: 0;
    flex-direction: column;
    gap: 3px;
  }

  .versions-method__copy strong {
    overflow: hidden;
    font-family: var(--font-display);
    font-size: 14px;
    line-height: 1.3;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .versions-method__copy small {
    overflow: hidden;
    color: #8f98a8;
    font-size: 11px;
    line-height: 1.35;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  [data-theme='dark'] .versions-method__copy small {
    color: #a3acbb;
  }

  .versions-method__arrow {
    color: #b0b7c3;
    font-size: 18px;
  }

  .versions-note {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    padding: 12px 14px;
    border-left: 3px solid var(--versions-accent);
    border-radius: 8px;
    background: rgba(var(--versions-accent-rgb), 0.08);
  }

  .versions-note__mark {
    display: grid;
    width: 20px;
    height: 20px;
    flex: 0 0 auto;
    place-items: center;
    border-radius: 50%;
    background: var(--versions-accent);
    color: #121831;
    font-family: Georgia, serif;
    font-size: 12px;
    font-weight: 700;
    line-height: 1;
  }

  .versions-note__copy {
    display: flex;
    flex-direction: column;
    gap: 2px;
    color: #737d8e;
    font-size: 11px;
    line-height: 1.45;
  }

  .versions-note__copy strong {
    color: #121831;
    font-family: var(--font-display);
    font-size: 12px;
  }

  [data-theme='dark'] .versions-note__copy {
    color: #aab2c0;
  }

  [data-theme='dark'] .versions-note__copy strong {
    color: #f9fafc;
  }

  @media (max-width: 640px) {
    .modal-card.modal-card--versions {
      max-height: min(92vh, calc(100dvh - max(20px, var(--safe-top)) - max(20px, var(--safe-bottom))));
      border-radius: 20px;
    }

    .modal-card--versions .modal-close {
      top: 20px;
      right: 20px;
      width: 38px;
      height: 38px;
    }

    .versions-modal {
      gap: 22px;
      padding: 28px 20px 24px;
    }

    .versions-modal__header {
      gap: 8px;
      padding-right: 46px;
    }

    .versions-modal__title {
      font-size: 26px;
    }

    .versions-modal__subtitle {
      font-size: 13px;
    }

    .versions-feature {
      grid-template-columns: 64px minmax(0, 1fr);
      align-items: start;
      gap: 16px;
      min-height: 0;
      padding: 22px 20px 24px;
      clip-path: polygon(0 0, 100% 0, 100% calc(100% - 18px), calc(100% - 18px) 100%, 0 100%);
    }

    .versions-feature__icon {
      width: 64px;
      height: 64px;
      border-radius: 18px;
    }

    .versions-icon svg {
      width: 38px;
      height: 38px;
    }

    .versions-feature h3 {
      font-size: 18px;
    }

    .versions-feature__actions {
      grid-column: 1 / -1;
      width: 100%;
    }

    .versions-methods__grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 420px) {
    .versions-feature {
      grid-template-columns: 1fr;
    }

    .versions-feature__icon {
      width: 58px;
      height: 58px;
    }

    .versions-feature__actions {
      display: grid;
      grid-template-columns: 1fr;
    }

    .modal-card--versions .versions-download,
    .modal-card--versions .versions-project {
      width: 100%;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .modal-card--versions .versions-download,
    .modal-card--versions .versions-project,
    .modal-card--versions .versions-method {
      transition: none;
    }

    .modal-card--versions .versions-download:hover,
    .modal-card--versions .versions-download:focus-visible,
    .modal-card--versions .versions-project:hover,
    .modal-card--versions .versions-project:focus-visible,
    .modal-card--versions .versions-method:hover,
    .modal-card--versions .versions-method:focus-visible {
      transform: none;
    }
  }
</style>
